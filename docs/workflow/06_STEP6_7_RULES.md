# 📋 Step 7-8: 규칙 생성 및 시각화 (Rule Generation & Visualization)

## 1. 개요 (Overview)

### 1.1 목적
클러스터 분석 결과를 바탕으로 제품 믹스 규칙을 생성하고 시각화합니다.

### 1.2 파일 위치
- **Step 7**: `src/step7_generate_rules.py` - 규칙 생성
- **Step 8**: `src/step8_visualize_rules.py` - 시각화
- **출력 위치**: `output/step7/`, `output/step8/`

---

## 2. Step 7: 규칙 생성 (Rule Generation)

### 2.1 처리 흐름도

```
┌─────────────────────────┐
│  Step 5-6 클러스터 결과   │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│  1. 클러스터 프로파일 분석            │
│  • 클러스터별 제품 분포 분석          │
│  • 상위 제품 식별                    │
│  • 특성 추출                         │
└───────────┬─────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│  2. 규칙 도출 (Rule Extraction)      │
│  • 클러스터별 권장 제품 비율          │
│  • IF-THEN 규칙 생성                 │
│  • 신뢰도/지지도 계산                │
└───────────┬─────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│  3. 규칙 검증 (Rule Validation)      │
│  • 규칙 일관성 검증                  │
│  • 커버리지 확인                     │
│  • 충돌 규칙 해결                    │
└───────────┬─────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│  4. 규칙 저장 (Rule Persistence)     │
│  • JSON 형식 저장                    │
│  • CSV 요약 테이블                   │
│  • 메타데이터 기록                   │
└───────────┬─────────────────────────┘
            │
            ▼
┌─────────────────────────┐
│  제품 믹스 규칙          │
└─────────────────────────┘
```

### 2.2 규칙 유형

| 규칙 유형 | 설명 | 예시 |
|-----------|------|------|
| **비율 규칙** | 제품 카테고리별 권장 비율 | "음료 30%, 스낵 25%, 유제품 20%" |
| **조건 규칙** | IF-THEN 형식 규칙 | "IF 도심 매장 THEN 음료 비중 증가" |
| **연관 규칙** | 제품 간 연관 관계 | "A 제품 구매 시 B 제품 함께 권장" |
| **제외 규칙** | 피해야 할 조합 | "저온 지역에서 아이스크림 비중 감소" |

### 2.3 규칙 구조

```json
{
    "rule_id": "R001",
    "cluster_id": 0,
    "rule_type": "proportion",
    "description": "고급 제품 선호 매장 규칙",
    "conditions": {
        "cluster": 0,
        "store_type": "premium"
    },
    "recommendations": [
        {"category": "premium_beverage", "proportion": 0.35},
        {"category": "organic_snack", "proportion": 0.25},
        {"category": "dairy", "proportion": 0.20},
        {"category": "others", "proportion": 0.20}
    ],
    "confidence": 0.85,
    "support": 0.20,
    "coverage": 200
}
```

### 2.4 규칙 생성 코드

```python
def generate_cluster_rules(
    cluster_profiles: Dict,
    matrix: pd.DataFrame,
    labels: np.ndarray
) -> List[Dict]:
    """
    클러스터별 제품 믹스 규칙을 생성합니다.
    
    Args:
        cluster_profiles: 클러스터 프로파일
        matrix: 피처 매트릭스
        labels: 클러스터 레이블
        
    Returns:
        규칙 리스트
    """
    rules = []
    
    for cluster_id, profile in cluster_profiles.items():
        # 상위 제품 추출
        top_products = profile["top_products"]
        
        # 비율 계산
        cluster_mask = labels == cluster_id
        cluster_matrix = matrix[cluster_mask]
        proportions = cluster_matrix.mean(axis=0)
        
        # 규칙 생성
        rule = {
            "rule_id": f"R{cluster_id:03d}",
            "cluster_id": cluster_id,
            "rule_type": "proportion",
            "recommendations": [
                {"product": prod, "proportion": float(proportions[prod])}
                for prod in top_products[:10]
            ],
            "confidence": calculate_confidence(cluster_matrix),
            "support": len(cluster_matrix) / len(matrix)
        }
        rules.append(rule)
    
    return rules
```

---

## 3. Step 8: 시각화 (Visualization)

### 3.1 처리 흐름도

```
┌─────────────────────────┐     ┌─────────────────────────┐
│  Step 7 규칙 데이터      │     │  Step 5-6 클러스터 데이터 │
└───────────┬─────────────┘     └───────────┬─────────────┘
            │                               │
            └─────────────┬─────────────────┘
                          │
                          ▼
┌─────────────────────────────────────┐
│  1. 클러스터 시각화                  │
│  • PCA 2D/3D 산점도                 │
│  • 클러스터 분포 히스토그램          │
│  • 실루엣 플롯                       │
└───────────┬─────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│  2. 규칙 시각화                      │
│  • 클러스터별 제품 비율 차트          │
│  • 히트맵                            │
│  • 레이더 차트                       │
└───────────┬─────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│  3. 지리적 시각화                    │
│  • 매장 위치 지도                    │
│  • 클러스터별 색상 구분              │
│  • 인터랙티브 맵                     │
└───────────┬─────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│  4. 대시보드 생성                    │
│  • HTML 인터랙티브 대시보드          │
│  • 정적 보고서 (PNG/PDF)            │
│  • 요약 테이블                       │
└───────────┬─────────────────────────┘
            │
            ▼
┌─────────────────────────┐
│  시각화 결과물           │
└─────────────────────────┘
```

### 3.2 시각화 유형

| 시각화 | 라이브러리 | 용도 |
|--------|-----------|------|
| **PCA 산점도** | matplotlib | 클러스터 분포 확인 |
| **히트맵** | seaborn | 제품-클러스터 관계 |
| **레이더 차트** | plotly | 클러스터 프로파일 비교 |
| **지도** | folium | 지리적 분포 |
| **대시보드** | plotly/dash | 인터랙티브 분석 |

### 3.3 시각화 예시

#### 3.3.1 클러스터별 제품 비율 차트

```
┌─────────────────────────────────────────────────────────────┐
│  Cluster Product Mix Comparison                             │
│                                                             │
│  100% ┤                                                     │
│       │  ████  ████  ████  ████  ████                       │
│   80% ┤  ████  ████  ████  ████  ████                       │
│       │  ████  ████  ████  ████  ████  ← Others             │
│   60% ┤  ░░░░  ████  ░░░░  ████  ░░░░                       │
│       │  ░░░░  ░░░░  ░░░░  ░░░░  ░░░░  ← Dairy              │
│   40% ┤  ▓▓▓▓  ░░░░  ▓▓▓▓  ░░░░  ▓▓▓▓                       │
│       │  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓  ▓▓▓▓  ← Snack              │
│   20% ┤  ▒▒▒▒  ▓▓▓▓  ▒▒▒▒  ▓▓▓▓  ▒▒▒▒                       │
│       │  ▒▒▒▒  ▒▒▒▒  ▒▒▒▒  ▒▒▒▒  ▒▒▒▒  ← Beverage           │
│    0% ┼──────────────────────────────────                   │
│         C0    C1    C2    C3    C4                          │
│                   Cluster                                   │
└─────────────────────────────────────────────────────────────┘
```

#### 3.3.2 클러스터 프로파일 레이더 차트

```
┌─────────────────────────────────────────────────────────────┐
│  Cluster Profile Radar Chart                                │
│                                                             │
│                    Beverage                                 │
│                       ▲                                     │
│                      /│\                                    │
│                     / │ \                                   │
│                    /  │  \                                  │
│        Snack ◄────●───●───●────► Dairy                      │
│                    \  │  /                                  │
│                     \ │ /                                   │
│                      \│/                                    │
│                       ▼                                     │
│                    Others                                   │
│                                                             │
│        ── Cluster 0    ── Cluster 1    ── Cluster 2         │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 출력 (Output)

### 4.1 Step 7 출력

| 파일명 | 형식 | 설명 |
|--------|------|------|
| `product_mix_rules.json` | JSON | 전체 규칙 데이터 |
| `rules_summary.csv` | CSV | 규칙 요약 테이블 |
| `rule_metadata.json` | JSON | 규칙 생성 메타데이터 |

### 4.2 Step 8 출력

| 파일명 | 형식 | 설명 |
|--------|------|------|
| `cluster_pca.png` | PNG | PCA 클러스터 시각화 |
| `product_heatmap.png` | PNG | 제품-클러스터 히트맵 |
| `cluster_radar.html` | HTML | 인터랙티브 레이더 차트 |
| `store_map.html` | HTML | 매장 위치 지도 |
| `dashboard.html` | HTML | 종합 대시보드 |

---

## 5. 실행 방법 (Execution)

### 5.1 명령어

```bash
# Step 7: 규칙 생성
python src/step7_generate_rules.py

# Step 8: 시각화
python src/step8_visualize_rules.py

# 특정 시각화만 생성
python src/step8_visualize_rules.py --type heatmap
python src/step8_visualize_rules.py --type map
```

### 5.2 예상 출력

```
[INFO] Starting Step 7: Rule Generation
[INFO] Loading cluster results from Step 5-6
[INFO] Analyzing cluster profiles...
[INFO] Generating rules for 5 clusters...
[INFO]   - Cluster 0: 3 rules generated
[INFO]   - Cluster 1: 4 rules generated
[INFO]   - Cluster 2: 3 rules generated
[INFO]   - Cluster 3: 5 rules generated
[INFO]   - Cluster 4: 3 rules generated
[INFO] Total: 18 rules generated
[INFO] Saving rules to output/step7/

[INFO] Starting Step 8: Visualization
[INFO] Creating PCA cluster plot...
[INFO] Creating product heatmap...
[INFO] Creating radar charts...
[INFO] Creating store map...
[INFO] Generating dashboard...
[INFO] All visualizations saved to output/step8/
```

---

## 6. 다음 단계

- **다음**: [07_CLIENT_REQUIREMENTS.md](./07_CLIENT_REQUIREMENTS.md) - 고객 요구사항 추적
- **이전**: [05_STEP4_5_CLUSTERING.md](./05_STEP4_5_CLUSTERING.md) - 클러스터링 분석
